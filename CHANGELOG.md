# 变更日志

本项目的每个版本都有一份独立的发版说明，含新功能详解、下载物与 SHA-256 校验和。
本文件只做索引；细节点进对应链接。

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)：`主版本.次版本.修订号`。
可下载的发布物只有终端编码智能体 `springai-code-tui`（自包含运行包，解压即用）。

| 版本 | 类型 | 摘要 |
| --- | --- | --- |
| [v1.22.0](docs/release-notes/v1.22.0.md) | 功能版 | **限额感知重试 + 大粘贴三件套 + MCP 运行期重载 + `-h/--help`**：① 智谱 GLM Coding Plan 撞 5h/7d 滚动限额（HTTP 429 业务码 1308/1310/1316-1321，重置时刻只内嵌在中文 message 文本里）时，旧行为是指数退避 ~1 分钟耗尽预算、回合失败、人肉重发——深夜挂机长任务在 02:01 被杀、05:00 才重置。现在 `QuotaLimitDetector` 沿 cause 链识别限额错误并解析重置时刻，回合**挂起等待**（不是失败）：输出区 `⏳ 限额等待：将于 15:30 自动重试（Esc 取消）` + 状态栏倒计时现算（45s/2m30s/3h5m），睡到重置点自动续跑（mid-stream 撞限不重放已下发内容、从断点续）；三条重试路径（L1 流式透明/L2 回合级续跑/子 agent 阻塞）经 `RetryPolicy` 单一真相源全覆盖，限额等待**不挤占普通重试预算**（reactor 计数不可重置，按次数扣减偏移），Esc 可取消（含串行子 agent——本版补齐其线程登记与 interrupt 短板）；近邻错误 1113/1309/1311（欠费/套餐过期/权限）等到天亮也不自愈，刻意不等。真机 429 payload 回归钉（1308 正例 + 1113 反例）+ PTY 端到端限额等待冒烟。② 大粘贴三件套（实报三连症状：内容被拆、日志 `\n` 变 Enter 触发「贴入即分批自动发送」、输入框随行数无限增高顶飞界面）：传输层 shadow `EventParser` 把括号粘贴「单次 50ms 读超时即掐断」放宽为**累计静默 500ms 预算**（大粘贴分块间隙超 50ms 即掐断、剩余字节回落普通按键解析是自动发送根源）+ 轮询 50ms→1ms（macOS pty 队列 ~1KB，50ms 轮询吞吐仅 20KB/s、4MB 粘贴 223s）；输入框侧 >400 字符或 12 行的粘贴折叠成 `[TEXTn]` 占位符（与 `[IMAGEn]` 同套路），全文登记、提交时展开，附件行提示「已折叠 N 段（共约 X 字）」，`/queue` 入队前展开、Esc 回填重新折叠、CRLF/裸 CR 归一 LF；回显侧实时回显与 `HistoryReplay` 重开回放统一折叠为前 2 行（单行再按 160 字符截断）+「…（已折叠，全文 N 行）」，只影响显示永不改发给模型的文本。③ MCP 配置运行期重载：改 `mcp.json` 无需重启，`/reload` 或 `/mcp` 面板 `r` 键热生效——按名字 diff 增量处理（新增后台连接/删除摘工具/参数变更换 Entry 重连/未变不闪断），`enabled` 意图以文件为准，忙碌时跳过 MCP 并提示，摘要「新增 X · 移除 Y · 重连 Z」。④ `-h/--help`：早于 key 检测（新机器无 key 可先看帮助），整串精确匹配防前缀误判，契约测试钉「帮助不撒谎」。⑤ 版权行统一署名 Xinghua Zhou；发版脚本支持 Gitee 镜像（`--remote gitee` 分流 + API v5） |
| [v1.21.7](docs/release-notes/v1.21.7.md) | 修订版 | **工具调用不再有隐藏的次数上限**：Spring AI 2.0.1 升级夹带的回合内限流（每工具 40 次、总量 150 次、撞限 `THROW`）从未被决策过，却在一次正常的大改动任务里把整回合打哑——`ToolCallLimitExceededException` 是 RuntimeException，绕过 `ResilientToolExecutionExceptionProcessor` 与 `ResilientToolCallingManager` 两道防线裸抛，`CodingAgent.handleError` 把回合标失败（与 v1.20.1 工具超时、v1.21.6 MCP 协议错误完全同构，第三次异常类型白名单教训）；子 agent 更糟——吃 `ChatClient` 自动注册的框架裸默认 manager，两道防线一道也没有。新增 `TurnToolLimitWiring` 作为主/子 agent **同源**的工具执行器工厂：**默认不限**（`unlimitedCallsPerTool` + `unlimitedTotalToolCalls`，框架那套 40/150/THROW 整个关掉），要熔断时 `CODETUI_MAX_CALLS_PER_TOOL` / `CODETUI_MAX_TOTAL_TOOL_CALLS` 可配（缺失/空白/非法/非正数一律视为不限），且 `onLimitExceeded` 固定 `RETURN_ERROR_RESPONSE`——撞限回错误文本让模型自纠（换 Write 整写 / 收尾汇报），绝不杀回合；`SubagentRunner` 改为显式挂同一工厂（顺带补上它此前没有的 Resilient 容错）。实测：生产进程内同回合连发 51 次 `Bash` 调用 0 次被拦、日志零 `Tool call limit` 命中，旧默认下第 41 次即杀回合 |
| [v1.21.6](docs/release-notes/v1.21.6.md) | 修订版 | **MCP 协议错误不再报废整回合 + OpenAI 系 60s 超时回归 + 图片放宽到 10 张**：① MCP server 返回 JSON-RPC error 时 `SyncMcpToolCallback` 裸抛 `McpError`（不包 `ToolExecutionException`），穿透 `DefaultToolCallingManager` 与 `ResilientToolCallingManager` 两道容错防线，一次工具失败即整回合报废（与 v1.20.1 超时事故同构：白名单防线再被新异常类型绕过）。生产实锤：Pencil 桌面 App 关掉文档后（App 未退，所有工具一律报「A file needs to be open in the editor」，连只读的 `get_app_state` 也拒），连续两个回合报废、失败调用未落盘、`-c` 恢复缺上下文，而这条错误本可由模型看到原文后提示用户自行消解。新增 `ResilientMcpToolCallback` 装饰器：catch `RuntimeException` 转信息完整错误文本回模型（点名工具+类型+消息+cause 链，不附纯 SDK 传输帧堆栈；`Error` 穿透不吞），装配在 `ToolEventCallback` 外（TUI 保留 ✗）`PermissionCallback` 内（权限语义不变）；错误走正常 tool result 路径，回合继续、正常落盘。附带确认 Pencil 侧行为并写入使用注意：接 agent 干活前 Pen.app 必须先开一个 .pen 文档（空文档也行）；② OpenAI 系 60s 总超时回归（与 ① 同源于 Spring AI 2.0.1 升级）：`OpenAiChatOptions.builder()` 默认 `timeout=PT1M` 且无法置回 null，`buildRequestOptions` 见非 null 即写入 `RequestOptions.timeout`，openai-java 每次 `newCall` 重建 OkHttp 超时——`callTimeout=60s` 是**总时长**语义，顶掉 base client 的 read/connect/write 四元组，大上下文+thinking+长输出的流式回合超 60 秒即被 watchdog cancel 成 `OpenAIIoException: Stream failed`，重试同限必挂（interview-drill 全量断流事故）；`OpenAiTimeouts.CHAT_OPTIONS_TOTAL_TIMEOUT=ZERO` 由四家 OpenAI 系 provider（openai/zhipu/qwen/opencode-go）三条路径（`defaultOptions` / `options(model)` / `options(model,thinking)`）显式携带，`Timeout.request=0` → `callTimeout(0)` 禁用总时长，空闲兜底交回 `StreamIdleTimeoutChatModel`（相邻两 chunk 之间，默认 300s），守卫测试四家 × 三路径钉死不回退 `PT1M`；③ 图片与拖图：用户图上限 `MAX_USER_IMAGES` 3→10（贴住最紧的 Qwen-VL 硬顶，Claude 100 / GLM-4V-Plus 5 / OpenAI ~16 全容得下）、`MAX_USER_TURN_DELIVERIES` 36→120、`MAX_REQUEST_TOKENS` 16k→32k（10 张满档 OpenAI 聚合网关 ≈28k，16k 时第 6 张即拒），拖图在输入框只插 `[IMAGE1]` 占位符（编号递增、Finder 多选各占一号，真实路径登记映射，提交正文保留占位符、引用块按 name 对号，提交/清空后复位），并修复「拖图紧贴已有文字不识别」（bracketed paste 与文字间无空白 → 整串成一个不存在路径、识别 0 张，`handlePasteEvent` 整段均为可识别图片路径时补边界空格） |
| [v1.21.5](docs/release-notes/v1.21.5.md) | 修订版 | **视觉链路计量校准 + DeepSeek 改名适配**：① 图片 token 口径按 provider 分家（新增 `ImageProfile`）并全部真机实测——DeepSeek 高估 3.6 倍修正（每请求上限 6000→16000，可容纳张数 3→25 量级）、聚合网关 OpenAI 通路低估 2–9.6 倍修正（`⌈w/32⌉⌈h/32⌉×1.2` 实测零误差）、智谱摆脱保守档（`min(6086,max(18,⌈w/28⌉⌈h/28⌉))`，差 3.7%）；② 图片改首次推一次（高水位线）+ 用户图/工具图独立回合额度（36/12 张·次），修回合中途图片消失与「重发吃光额度饿死 Read」，额度耗尽如实写 `turn_budget_exhausted` 不再说「Read 一次就能看」；③ DeepSeek 2026-09-21 把 flash 与 flash-vision 合并为 `deepseek-flash`（已是思考模型），清单 3→2 项，`model.json` 旧名读取时自动升级（不迁移会静默失去视觉）；④ 升级 Spring AI 2.0.1（修流取消不释放连接，SDK 对齐 + 显式 httpclient5 + 删千问冗余补丁）、JUL 桥接 SLF4J（OkHttp 告警不再打乱输入框） |
| [v1.21.4](docs/release-notes/v1.21.4.md) | 修订版 | **修复折行末字符丢失**：Terminal.app 中正文写满终端宽度后，行尾 `ESC[K` 会擦掉最后一个字符；改为正文前清行、正文后 CR/LF，不缩减显示宽度。补齐满宽 ASCII、中文、跨样式文本、续行与空行输出回归测试，用户实机确认修复 |
| [v1.21.3](docs/release-notes/v1.21.3.md) | 修订版 | **重试退避升级 + 压缩边界修复**：① 三层重试的退避序列从 500ms×2ⁿ 封顶 4s（总尝试 5）整体拉长为 1s×2ⁿ 封顶 30s（总尝试 7，L1 流式 `L1_RETRIES=6` / 子 agent `MAX_ATTEMPTS=7` / L2 续跑 `L2_RESUMES=4`），网关坏窗口以十秒计，短退避原先常在服务端恢复前把尝试用光；并新增采信服务端 `Retry-After` 头（429/529，delta-seconds 与 HTTP-date 两种格式，封顶 60s，无头退化纯指数），`WebClientResponseException` 与 `OpenAIServiceException` 两条异常链都覆盖。退避与延迟计算收敛到 `RetryPolicy` 作唯一真相源（新增 `nextDelayMs`/`retryAfterMs`/`backoffRetry`，L1/L2 共用 reactive `Retry` 工厂替代分散的 `Retry.backoff` 与 `resumeBackoffMs`；阻塞与 reactive 两条退避共用同一测试压缩钩子），UI 文案改按 `maxAttempts==7` 区分 `(x/7·传输)` / `(x/4·续跑)`。② 修复自动压缩偶发 Anthropic 400「unexpected tool_use_id」——`BoundedSummarizationCompactionStrategy` 边界回退只判 `isRootEvent()`，但该方法在单 agent 会话对每条事件恒 true，回退沦为空操作，切割点可能落在 `tool_use` 与其 `tool_result` 之间造成孤儿块；抽出 `isTurnStart()`（root 且 USER 消息，对齐框架 `CompactionUtils.snapToTurnStart`），两处分支统一 |
| [v1.21.2](docs/release-notes/v1.21.2.md) | 修订版 | **`/model` 选择器反复开关残影 + 输入框消失 + `/continue` 不再清空 todo 面板**：排查真实用户报告（截图实锤），查明三处独立问题叠加：① `InlineDisplay` 收缩 live 区且不走整体平移时，删除起点错用「顶部稳定内容 + 底部稳定内容行数之和」，顶部有 todo 面板时这个起点落进变化区域中间，该删的整段只删一半，前几行永远漏删、卡在屏幕上——改为按内容比对现算「顶部真没变的行数」；② `/model` 选择器此前不设行数上限（目录已有 20 个模型），加上 todo 面板容易让 live 区超过终端实际高度，光标相对移动被物理边界截断后位置计算全错，表现为 Esc 关闭后输入框整段消失、要等下一次不相关事件才重新出现——照抄技能选择器已有的窗口化做法加了上限（`MODEL_PICKER_CAP=10`，随选中项自动滚动）；③ 上面①的「顶部真没变行数」贪心比对有隐藏 bug：斜杠命令补全菜单过滤词收紧时最靠前的匹配项文字恰好不变，会被误判成稳定前缀的一部分，删除起点多算一行——加了尾部整体位移校验兜底。另外 `/continue` 续跑不再清空刚恢复/中断前的 todo 面板（`ConversationState.preserveTodoOnNextTurn`，此前 `-c` 恢复面板修复被这个新回合无条件清空的问题抵消了） |
| [v1.21.1](docs/release-notes/v1.21.1.md) | 修订版 | **子 agent TodoWrite 可见性与 `-c` 恢复面板重建**：排查真实用户报告"子 agent 一直用 TodoWrite、主 agent 却不用"，查明是两处已有设计叠加：① `general-purpose` 子 agent 调 TodoWrite 的内容此前就会被 UI 有意丢弃（只留一行 scrollback 面包屑），继续默认放行只是白白多花一次工具往返，现改为默认 deny；② `-c` 恢复会话时 `ConversationState.replayHistory` 从不重建 todo 面板，恢复后面板会一直空白直到主 agent 恰好再调一次 TodoWrite，现从历史最后一次 TodoWrite 调用重建（`HistoryReplay.lastTodoSnapshot`，复用既有 `AgentTools.toLines` 格式化，不新写一份）。两处均 TDD + spec 合规审查 + 代码质量审查，另加一轮覆盖组合效果的整体审查（顺带揪出并修正两处过时文档，含一段描述反了核心机制的 `AgentListener` javadoc） |
| [v1.21.0](docs/release-notes/v1.21.0.md) | 功能版 | **子 agent 按次覆盖模型 + drain 批时间预算跨段共享修复**：`Task`/`ParallelTasks` 新增可选 `model` 参数（`provider:modelId`），主 agent 可把同一份材料并发派给不同模型的子 agent 分别评估、再综合对比；跨 provider 精确路由，未知模型清楚报错（顺带修复 `ProviderRegistry.requestSelection` 旧 bug——两分支殊途同归，跨 provider modelId 曾被静默按当前激活模型跑掉）；scrollback 与任务面板（含后台）显示每次委派实际用的模型。另修复"输出时打字卡死"的又一次复发——`AsyncPtyWriter`（v1.19.2）解决了 pty 写阻塞，但 diff 展开与表格排版这两处已知预算例外的豁免计数器是每次 `drain()` 调用局部重置，`CodeTuiView` 一批最多调 3 次、每段各自重新豁免，成本可跨段叠加；现改按整批累计判断，只在批的第一段生效一次。工具描述同时收紧模型覆盖使用边界（真实会话实证：主 agent 曾把"换模型"当网络瞬时故障的自救手段，用户没有要求）——只有用户明确要求时才该用，瞬时故障应原地重试同一 dispatch |
| [v1.20.1](docs/release-notes/v1.20.1.md) | 修订版 | **两个生产事故修复（回合存活）**：① 工具超时/中断不再杀整回合——`TimeLimitedToolCallback` 超时分支曾抛 `IllegalStateException` 穿透两道工具容错防线（`DefaultToolCallingManager` 只接 `ToolExecutionException`、`ResilientToolCallingManager` 只接工具名解析失败），Brave 搜索 20 秒超时即报废整回合连同已完成进度；改包 `ToolExecutionException`（携带真实工具定义）后落进 `ResilientToolExecutionExceptionProcessor` 转错误文本回模型自纠（换路/换参/说明），回合继续，回给模型的文案不变，中断分支同修。② 网关 503 等 SDK 系瞬态 5xx 进入自动重试——`RetryPolicy` 的「网关 5xx」判据只认 `WebClientResponseException`，智谱/Qwen 等 openai-java 栈 provider 的 503 抛 `InternalServerException`（非 IOException、message 无关键词）四条判据全落空，实测 60 次直接杀回合且 API 恢复后不自愈；`shouldRetry` 新增 `OpenAIServiceException` 镜像分支（≥500 或 429 瞬态可重试、其余 4xx 红线否决），判据唯一真相源——主 agent L1/L2 与子 agent路径自动生效，取消/中断红线不动，`OpenAIRetryableException` 不越权（SDK 自带 2 次重试）。两修复均 TDD（真 SDK 异常类型、生产传播形态包装） |
| [v1.20.0](docs/release-notes/v1.20.0.md) | 功能版 | **主 agent 流式两层重试 + ↻ 界面重试提示 · GPT-6 Astra 新默认 · Markdown 表格渲染 · 项目名常驻状态行**：L1 传输层零下发透明重试（`RetryingStreamChatModel`，判据/退避与子 agent 同源 `RetryPolicy`）+ L2 回合级续跑（mid-stream 断流在 defer 内重建 spec 重走 advisor 管线，按会话尾部形状分流：文本段断重放单条 user / 工具中途断走插话 notice 通道）+ `CODETUI_STREAM_RETRY` 计费总闸（all/l1/l2/off；⚠ `l2` 档当前真实链 ≈ off——SII 唯一生产源是 L1 classify，L1 不包装则主链无 SII、L2 filter 永不命中，档位为 API 前瞻，待 classify-only 的 L1 变体）；每次重试经 `onRetryScheduled`（5 参事件）显示 `↻ 重试中 (x/5·传输)/(x/2·续跑)` 提示行 + RETRYING 状态波光（80 列宽度预算）；续跑安全前提实测钉死（advisor 链破坏性消费 → defer 内重建是唯一重订阅形态；blank user 三处清除；Esc 退避期 dispose 无幽灵请求；耗尽失败文案拼「已自动重试…仍失败：根因」、零重试错误不加前缀）；`gpt-6-astra` 入 OpenAI 清单并成默认（effort 五档无 none；1.05M 窗口；视觉前缀精确到 astra 系；无 early access 报不可用时切回 gpt-5.6）；Markdown 表格块渲染（表头加粗 + `─` 分隔线 + 显示宽度对齐，超宽削列/格内折行不丢字，块缓冲状态机 + Esc 排空，任一输入不抛异常——修复模型表格在终端 CJK 错位）；项目名 ` · <目录名>` 常驻状态行行尾（五种常态行，不进宽度预算、窄终端先截项目名不挤键位提示；超 24 显示宽截断），tab 标题退回纯提醒（⏳/✓ + BEL，平态纯品牌串）；ExpectedLog 断言式日志接管测试基建 |
| [v1.19.2](docs/release-notes/v1.19.2.md) | 修订版 | **根治「模型输出时打字卡死」**：pty 终端写剥离到专职守护线程（AsyncPtyWriter，两级字节预算 + 延迟批 FIFO + 清屏屏障 + 设备死亡检测），渲染线程不再睡死在 write(2) 上——输出高峰期间打字/插话照常处理；超预算单批分片入队（小帧延迟不再被单个超批放大，分片边界避开 UTF-16 代理对防内容损坏）；/clear 清屏屏障降级时补独立说明行（解释分割线上方的旧内容残留）；读端停摆冒烟（真 pty 复现内核缓冲写满 + 顺序型判据 + 证红开关）；JLineBackend.writer 结构钉（探针反射链升级红灯）；终端注意提示（OSC/BEL）改经 writer 队列（不再与内容抢 PrintWriter 锁） |
| [v1.19.1](docs/release-notes/v1.19.1.md) | 修订版 | **Anthropic 新模型 Claude Fable 5.1 接入**：`claude-fable-5-1`（最强旗舰 · 1M 上下文 / 128K 输出 · 思考 always-on 不可禁用）入内置清单（默认仍 `claude-opus-5`，官方建议多数负载从 Opus 5 起步），`claude-fable-5` 转「上代旗舰」保留在清单末尾；内置上下文窗口补 `anthropic:claude-fable-5-1` → 1M；thinking 不可禁用判定从精确匹配改为 `claude-fable-` 前缀匹配（不再漏后续 5-x 版本）；`config.env.example` 的 `ANTHROPIC_MODELS` 示例同步 |
| [v1.19.0](docs/release-notes/v1.19.0.md) | 功能版 | **界面更新改事件驱动 + 智谱视觉模型全面接入**：删除常驻 100ms 重绘与 drain 轮询，改为合并的状态变更通知（coalesced），空闲零输出、高频 token 合并不再逐 token 排队，预览/缩放稳定/动画/上下文刷新/输出续帧全部按需一次性调度，物理行输出预算改按 segment 级懒折行计；智谱内置清单加 `glm-5.3-flash`（原生多模态 · 1M 上下文 · 1/10 价，默认仍旗舰 `glm-5.3` 不变），视觉名单补 `glm-4.6v` / `glm-5v` 前缀（纯文本 `glm-5.3` 不误伤），内置上下文窗口（flash 1M / 4.6v 128K / 4.5v 64K，未核实窗口落保守兜底），thinking 三档；视觉真机冒烟探针（纯图 / 流式工具 / 图+工具复合）；修复状态栏波光续帧竞态等一批 UI 竞态 |
| [v1.18.3](docs/release-notes/v1.18.3.md) | 修订版 | **输出期打字卡死与终端崩溃根治 + agent 包按功能重构**：每帧 pty 写入限速改按物理行计（此前 300 的额度是逻辑 `OutputLine` 条数，一条折行/diff 展开成十几到上百物理行，实测一帧写出 4500 行），并给此前完全没接限速的流式出口补上预算（实测一帧 5000 行）——渲染线程不再被单帧占住数百毫秒，macOS Terminal.app 不再被几百 KB 的 pty 突发推进自身 use-after-free；`codetui.agent` 顶层 88 个类按功能拆进 10 个子包（`llm`/`mcp`/`subagent`/`skill`/`session`/`compaction`/`seam`/`tools`/`prompt`/`interjection`），顶层只留 `CodingAgent` 与 `AgentTools`，无方法体改动、行为无变化；16 个 main 包补齐 `package-info`（写明各包职责、依赖方向与拆包理由，含 2 条已知包循环与提权清单）。`docs/` 下 2026-08-28 之前的历史文档里的 `codetui.agent.*` 类路径按当时结构书写，未随本次调整更新。 |
| [v1.18.2](docs/release-notes/v1.18.2.md) | 修订版 | **OpenCode Go 官方视觉模型 + Provider 能力隔离**：内置 `deepseek-v4-flash-vision-exp`，只为网关已确认的模型开放图片；UI 附件闸门与请求兑现统一按 provider+model 判定，避免聚合网关同名模型串用视觉能力；消息在飞、排队或待送达插话期间禁止切模型，避免图片静默降级；补齐图片处理实现原理与公共 API 文档 |
| [v1.18.1](docs/release-notes/v1.18.1.md) | 修订版 | **DeepSeek 视觉图片可靠送达 + 二进制外置 fail-closed**：修复工具多响应被 Spring AI 展开后图片注册表按绝对下标错位、模型只看到引用而看不到图；项目外图片 Read 会复制进 artifacts 再引用，PNG/PDF hexdump 不再进入模型上下文；外置失败时 fail-closed 移除二进制内容；artifacts 副本 Read 与用户附件统一内容哈希 id，避免同一张图被当作两张 |
| [v1.18.0](docs/release-notes/v1.18.0.md) | 功能版 | **`/context` 上下文占用透明化 + 工具调用报错不再毁掉回合**：报告升级为分类明细（事件按用户/助手/工具分桶，token 统一为「上下文占用」口径=系统提示词+会话消息，构成逐行占比、总和恒 100%）；工具名拼错/工具执行异常改为把错误原因回给模型自纠（不新增别名工具），MCP 工具为空不再覆盖 defaultTools；长期记忆系统提示精简省 34% token，系统提示词补「动手前查技能清单、需求未定须提问」；并入 v1.17.2 的「输出中打字」卡死/终端崩溃修复（token 估算移出渲染线程、残行 20 万上限、渲染降频、预览节流） |
| [v1.17.1](docs/release-notes/v1.17.1.md) | 修订版 | **根治行内 TUI 卡死**：`/model` 切换模型长时间不选择再回车、对话输入后「思考中」界面永久定格、任何按键失效——根因是依赖库 `tamboui-tui` 的 `InlineTuiRunner.run()` 事件循环对 `handler.handle`/`UiRunnable.run` 零 try-catch（全屏版 `TuiRunner` 有），异常逃出循环即杀死渲染线程；现以同包同名 shadow 类给事件循环包 `try(Throwable)`（记录日志并 continue），渲染线程永不因 handler 异常死亡 |
| [v1.17.0](docs/release-notes/v1.17.0.md) | 功能版 | **DeepSeek 视觉模型支持**：`deepseek-v4-flash-vision-exp` 入内置清单（`/model` 可切，默认仍 v4-pro），视觉能力按前缀精确判定、未知模型不发图；修复 spring-ai-deepseek 2.0.0 序列化静默丢图（HTTP 层改写 content → text + image_url/file 块，无图请求逐字节不变、fail-open）；双传输通道（默认 base64 内联，`DEEPSEEK_VISION_TRANSPORT=files` 走 Files API，sha 幂等 + 失败自动降级内联）；沿用「图片只活当轮、单回合硬上限」纪律，每图最多 384 token 计费 |
| [v1.16.0](docs/release-notes/v1.16.0.md) | 功能版 | **终端 tab 会主动提醒，手动压缩恢复保留 20 事件语义**：回合完成或问询/审批等待用户时写 OSC 0/2 tab 标题并发送 BEL，Esc 取消不误响、按键恢复默认标题；修复 glm-5.3 / 1M 窗口下手动 `/compact` 因 token 预算过大而误报「无可压缩内容」，现在长会话即使低于 token 目标也会按最近 20 事件强制归档 |
| [v1.15.0](docs/release-notes/v1.15.0.md) | 功能版 | **长会话压缩降为一次调用、子 agent 重试真正生效**：自动压缩改乐观全量摘要（约 8 次调用 → 1 次），窗口虚高时按真实失败校准容量（数字锚定一步到位 / 减半探测），knownGood/knownBad 区间按模型跨压缩记忆、绝不重发注定失败的请求，20 次调用硬上限 + 本地兜底永不失控；glm-5.3 入内置清单（默认旗舰，1M 窗口，thinking 不可禁用）；子 agent 重试判定对齐生产日志四类瞬态故障（此前重试层 0 触发、失败全冒泡），改指数退避 |
| [v1.14.1](docs/release-notes/v1.14.1.md) | 修订版 | **界面可见性两修**：模型输出期间输入框不再反复闪出第二条边框（live 区改在顶部增删行，终端写入量 422KB→131KB；该缺陷自 v1.13.0 起存在）；深色终端下次要文字不再近乎不可见（ANSI 亮黑改为 256 色灰阶三档，引用块提到信息行同级，代码块左边栏的 truecolor 改 indexed）；新增调色板对比度单测与流式输入框 pty 实机冒烟 |
| [v1.14.0](docs/release-notes/v1.14.0.md) | 功能版 | **流式调用不再无限卡住，缓存状态更易扫读**：六家 provider 统一增加流式空闲超时（默认 300 秒，可配置）；思考与工具运行期间继续显示缓存命中率；窄终端按信息优先级保留动态状态，缓存命中百分比值单独高亮 |
| [v1.13.0](docs/release-notes/v1.13.0.md) | 功能版 | **缓存命中率可见**：`/context` 新增命中率行（命中/计费输入原值），状态栏常驻「缓存命中 N%」；DeepSeek 流式补 `include_usage`、usage 终止前原子提交、缓存 token 经 nativeUsage 兜底；修复状态栏超宽时动态数据被 80 列终端截断（模型名+动态数据保完整，静态键位提示让位）；`CODETUI_CO_AUTHOR` 提交署名（默认关闭），`config.env` 改逐行解析杜绝 shell 注入 |
| [v1.12.0](docs/release-notes/v1.12.0.md) | 功能版 | **OpenCode Go 思考档位按模型实测并扩充模型清单**：`reasoning_effort` 从一刀切三档改为按模型返回真实档位（DeepSeek/GLM/Kimi/MiniMax/混元旗舰可用到 `max`），模型清单 15 → 22（补入 10、剔除 3 项上游不可用）；修复 CI 构建/打包命令缺 `-am` |
| [v1.11.1](docs/release-notes/v1.11.1.md) | 修订版 | **自动压缩不再过早触发**：修复 OpenCode Go 网关同名模型上下文窗口误判（真实 1M 窗口被按 128K 算，新会话频繁触发压缩），按 modelId 同名回退复用原厂窗口；修正构建/打包命令文档（补 `-am` 与 `-Dsurefire.failIfNoSpecifiedTests=false`） |
| [v1.11.0](docs/release-notes/v1.11.0.md) | 功能版 | **新增 OpenCode Go provider**：一把 `OPENCODE_GO_API_KEY` 接通 MiniMax / Kimi / GLM / DeepSeek / 通义 / MiMo / 混元 / OpenAI / xAI 等聚合网关模型，复用 OpenAI 兼容通路接入，思考强度走 `reasoning_effort` 三档（low/medium/high）；**修复同名模型串号**（模型身份升级为 provider+model，`/model` 与思考设置精确区分来源）；**修复多行粘贴被拆成多次提交**（开启 bracketed paste，整段粘贴一次进入输入框，仅手动 Enter 发送） |
| [v1.10.0](docs/release-notes/v1.10.0.md) | 功能版 | **模型思考设置**：`/model` 列表按 `→` 为每个模型独立配置思考模式与强度（DeepSeek `reasoning_effort`、Anthropic/OpenAI effort、通义 token 预算、智谱 effort），按模型记忆；另有行内差分渲染与 IME 损坏修复、会话预压缩、ChatClient 合并链修复 |
| [v1.9.1](docs/release-notes/v1.9.1.md) | 修订版 | **`/skill` 大量技能不再闪动**：技能选择器改为固定高度的跟随窗口，避免终端反复滚动重排；仍可遍历并挂载全部技能；同步理顺发布包下载运行入口与 JDK 17+ 说明 |
| [v1.9.0](docs/release-notes/v1.9.0.md) | 功能版 | **后台任务不再像卡死**：`/tasks` 面板 RUNNING 行加波光动画，一眼看出还在跑；后台完成通知明确要求模型继续 Todo 计划（而非仅确认结果）；`run_in_background` 使用条件写进工具描述，默认前台、减少误用后台 |
| [v1.8.3](docs/release-notes/v1.8.3.md) | 修订版 | **context 低估修复 + Terminal.app 崩溃缓解**：`/context` token 估算正确纳入工具输出（`ToolResponseMessage.getText()` 空串 bug）；每帧 pty 写入限速 300 行，降低 Terminal.app GCD kevent 崩溃频率；README 拆分为 docs/guide/ 专题文档 |
| [v1.8.2](docs/release-notes/v1.8.2.md) | 修订版 | **竞态修复 + 日志统一**：忙时提交在回合边界不再被误路由为排队（原子快照消竞态窗口）；全部有意义 debug 日志升 info，错误路径补 error；系统提示工具访问边界措辞放宽为「服从权限引擎」 |
| [v1.8.1](docs/release-notes/v1.8.1.md) | 修订版 | **缩放窗口三连修**：拖动终端宽度不再打烂界面（拖拽中原地清扫 + 停稳后整屏重放，回滚缓冲不再堆残骸）；中文输入法拼音不再「错位」到输入框边框上；模型回复超宽行折行续排、不再被截断（缩放后按新宽度重新折行，内容不丢） |
| [v1.8.0](docs/release-notes/v1.8.0.md) | 功能版 | **不用干等**：后台子 agent（`run_in_background`，派出去的活不占回合，`/tasks` 管理）、**回合中插话**（忙时 `Enter` 直接把话插进下一次模型调用，不必 Esc 砍掉整个回合）、记住上次用的模型（`.codetui/model.json`）；**`BYPASS` 进了 `Shift+Tab` 循环**（⚠️ 不再需要启动参数——**安全性净下降**，升级前请读发版说明） |
| [v1.7.0](docs/release-notes/v1.7.0.md) | 功能版 | **视觉输入**：模型真能看见图片（你贴的 + 工具产的），图从不进会话记忆、单回合花费有硬上限；**`--dangerously-skip-permissions` 改成名副其实**（⚠️ 它此前并不真的跳过全部检查，本版起只剩 deny 规则——**安全性净下降**，升级前请读发版说明） |
| [v1.6.0](docs/release-notes/v1.6.0.md) | 功能版 | **权限管理**：执行前审批面板、`permissions.json` 规则、计划模式、内置底线（⚠️ 默认行为变了，升级前请读发版说明）。*注：其中「BYPASS 也盖不住内置底线」一条已在 v1.7.0 变更* |
| [v1.5.0](docs/release-notes/v1.5.0.md) | 功能版 | 联网搜索（博查 + Brave 两家共存）、MCP 远程传输（Streamable HTTP）、Claude Opus 5 设为 Anthropic 默认模型 |
| [v1.4.0](docs/release-notes/v1.4.0.md) | 功能版 | 通义千问 provider、`*_MODELS` 可配置模型清单、`/mcp` 运行期 MCP 管理 |
| [v1.3.1](docs/release-notes/v1.3.1.md) | 修订版 | — |
| [v1.3.0](docs/release-notes/v1.3.0.md) | 功能版 | 含完整功能全景与⚠️安全声明 |
| [v1.2.1](docs/release-notes/v1.2.1.md) | 修订版 | — |
| [v1.2.0](docs/release-notes/v1.2.0.md) | 功能版 | — |
| [v1.1.0](docs/release-notes/v1.1.0.md) | 功能版 | — |
| [v1.0.0](docs/release-notes/v1.0.0.md) | 首个发布 | 含完整功能全景与⚠️安全声明 |

> 初次接触本项目，建议先读 [v1.3.0](docs/release-notes/v1.3.0.md) 与 [v1.0.0](docs/release-notes/v1.0.0.md)
> ——功能全景与安全声明在这两份里最完整；其余版本的说明只列相对上一版的差异。
